## 1. Foundation

- [x] 1.1 Create `pkg/m3tokens/` package directory and `go.mod` module structure
- [x] 1.2 Define shared types: `TokenState`, `Component`, token category interfaces
- [x] 1.3 Create JSON schema for extractor output (`schema.json` format definition)
- [x] 1.4 Add `go generate` directive to `main.go` for the generator pipeline

## 2. Schema Extraction

- [x] 2.1 Build Sass bridge code to extract `$supported-tokens` from `_md-comp-*.scss` via `godartsass`
- [x] 2.2 Parse extracted token names to identify state prefixes, categories, and properties
- [x] 2.3 Parse system modules (`md-sys-color`, `elevation`, `shape`, `state`, `typescale`) for static values
- [x] 2.4 Build dependency graph from `$_default` maps in component files
- [x] 2.5 Write `schema.json` containing all component schemas and system token maps
- [x] 2.6 Add validation that `schema.json` conforms to the defined JSON Schema
- [x] 2.7 Handle `$renamed-tokens` and alias mappings in the schema

## 3. Token Code Generation

- [x] 3.1 Generate `component_enums.go` with `Component`, `TokenCategory`, `TokenProperty`, `TokenState` enums
- [x] 3.2 Generate base token structs per component (e.g., `FilledButtonTokens` with `Container`, `Icon`, `LabelText`)
- [x] 3.3 Generate state overlay structs per component/state combination (e.g., `FilledButtonHoverOverlay`)
- [x] 3.4 Generate system token maps (`sysColor`, `sysElevation`, `sysShape`, `sysState`, `sysTypeScale`)
- [x] 3.5 Generate `Resolver` type with component-specific resolution methods
- [x] 3.6 Generate `Theme` struct and `NewTheme(mode string)` constructor
- [x] 3.7 Generate `FlatMap()` compatibility method using struct tag reflection
- [x] 3.8 Add generated file header comments (`// Code generated by m3-token-gen. DO NOT EDIT.`)

## 4. State Resolution Engine

- [x] 4.1 Implement base fallback semantics in resolver methods (return base if state overlay lacks value)
- [x] 4.2 Implement nil-safe overlay access (nil overlay -> base, nil field -> base)
- [x] 4.3 Implement compound state resolution with priority order (checking `Disabled` before `Hover`, checking compound overlays before individual)
- [x] 4.4 Add unit tests for all state fallback scenarios
- [x] 4.5 Add unit tests for compound state resolution

## 5. Custom Component Support

- [ ] 5.1 Add `custom/tokens/` directory scanning to the schema extractor
- [ ] 5.2 Generate typed structs for custom components identical to M3 components
- [ ] 5.3 Implement cross-component alias resolution in generated code
- [ ] 5.4 Add support for custom state vocabularies in `TokenState` generation
- [ ] 5.5 Add unit tests for custom component generation and resolution

## 6. Verification & Migration

- [ ] 6.1 Verify generated flat map matches existing `theme/tokens.go` for all 6 modes
- [ ] 6.2 Write integration test that compiles generated code and resolves tokens
- [ ] 6.3 Benchmark resolver performance vs. flat map lookups
- [ ] 6.4 Update `README.md` with typed token system documentation
- [ ] 6.5 Add example consumer code showing `theme.FilledButton().LabelTextColor(StateHover)` pattern

## 7. Cleanup

- [ ] 7.1 Remove old flat-map generation path from `main.go` once verified
- [ ] 7.2 Mark `theme/tokens.go` as deprecated in comments
- [ ] 7.3 Ensure all generated code passes `go vet` and `gofmt`
