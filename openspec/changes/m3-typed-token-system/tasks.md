## 1. Foundation

- [ ] 1.1 Create `pkg/m3tokens/` package directory and `go.mod` module structure
- [ ] 1.2 Define shared types: `TokenState`, `Component`, token category interfaces
- [ ] 1.3 Create JSON schema for extractor output (`schema.json` format definition)
- [ ] 1.4 Add `go generate` directive to `main.go` for the generator pipeline

## 2. Schema Extraction

- [ ] 2.1 Build Sass bridge code to extract `$supported-tokens` from `_md-comp-*.scss` via `godartsass`
- [ ] 2.2 Parse extracted token names to identify state prefixes, categories, and properties
- [ ] 2.3 Parse system modules (`md-sys-color`, `elevation`, `shape`, `state`, `typescale`) for static values
- [ ] 2.4 Build dependency graph from `$_default` maps in component files
- [ ] 2.5 Write `schema.json` containing all component schemas and system token maps
- [ ] 2.6 Add validation that `schema.json` conforms to the defined JSON Schema
- [ ] 2.7 Handle `$renamed-tokens` and alias mappings in the schema

## 3. Token Code Generation

- [ ] 3.1 Generate `component_enums.go` with `Component`, `TokenCategory`, `TokenProperty`, `TokenState` enums
- [ ] 3.2 Generate base token structs per component (e.g., `FilledButtonTokens` with `Container`, `Icon`, `LabelText`)
- [ ] 3.3 Generate state overlay structs per component/state combination (e.g., `FilledButtonHoverOverlay`)
- [ ] 3.4 Generate system token maps (`sysColor`, `sysElevation`, `sysShape`, `sysState`, `sysTypeScale`)
- [ ] 3.5 Generate `Resolver` type with component-specific resolution methods
- [ ] 3.6 Generate `Theme` struct and `NewTheme(mode string)` constructor
- [ ] 3.7 Generate `FlatMap()` compatibility method using struct tag reflection
- [ ] 3.8 Add generated file header comments (`// Code generated by m3-token-gen. DO NOT EDIT.`)

## 4. State Resolution Engine

- [ ] 4.1 Implement base fallback semantics in resolver methods
- [ ] 4.2 Implement nil-safe overlay access (nil overlay → base, nil field → base)
- [ ] 4.3 Implement compound state resolution with priority order
- [ ] 4.4 Add unit tests for all state fallback scenarios
- [ ] 4.5 Add unit tests for compound state resolution

## 5. Custom Component Support

- [ ] 5.1 Add `custom/tokens/` directory scanning to the schema extractor
- [ ] 5.2 Generate typed structs for custom components identical to M3 components
- [ ] 5.3 Implement cross-component alias resolution in generated code
- [ ] 5.4 Add support for custom state vocabularies in `TokenState` generation
- [ ] 5.5 Add unit tests for custom component generation and resolution

## 6. Verification & Migration

- [ ] 6.1 Verify generated flat map matches existing `theme/tokens.go` for all 6 modes
- [ ] 6.2 Write integration test that compiles generated code and resolves tokens
- [ ] 6.3 Benchmark resolver performance vs. flat map lookups
- [ ] 6.4 Update `README.md` with typed token system documentation
- [ ] 6.5 Add example consumer code showing `theme.FilledButton().LabelTextColor(StateHover)` pattern

## 7. Cleanup

- [ ] 7.1 Remove old flat-map generation path from `main.go` once verified
- [ ] 7.2 Mark `theme/tokens.go` as deprecated in comments
- [ ] 7.3 Ensure all generated code passes `go vet` and `gofmt`
